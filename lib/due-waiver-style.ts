/**
 * Estilo del chip EXENTA — distinto del de PAGADA (`secondary`) a propósito:
 * una cuota exenta no se pagó. Vive en un módulo sin "use client" para poder
 * usarlo tanto desde componentes de servidor como de cliente.
 */
export const WAIVED_BADGE_CLASS = "border-sky-300 bg-sky-50 text-sky-800 dark:border-sky-700 dark:bg-sky-950 dark:text-sky-200";
